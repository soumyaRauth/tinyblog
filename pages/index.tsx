import Header from "@/Components/Header/Header";

/**
 *
 * @returns header and blog posts
 */
const HomePage = () => {
  return <Header title="master of puppet" format="default" />;
};

export default HomePage;
